import 'package:flutter/cupertino.dart';

class SitingsScreen extends StatelessWidget {
  const SitingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
